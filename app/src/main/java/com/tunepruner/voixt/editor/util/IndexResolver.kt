package com.tunepruner.voixt.editor.util

/** This class will be in charge of resolving the index that will
 * be needed to insert text at the correct position in a text body. */
class IndexResolver {
    fun resolveIndexFromTimestamp(timestamp: Long): Int = TODO("")
}