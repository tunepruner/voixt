package com.tunepruner.voixt.editor.util

import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp

object StringLengthResolver {
    fun resolveStringToDp(string: String): Int {
        TODO("I don't know how to resolve string length yet!")
        // Since I will be using a monospaced font, this should be feasible.
        return 0
    }
}